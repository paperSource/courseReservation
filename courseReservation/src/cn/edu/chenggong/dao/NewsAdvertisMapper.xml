<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace命名空间，作用就是对sql进行分类化管理，理解sql隔离 
注意：使用mapper代理方法开发，namespace有特殊重要的作用
-->

<mapper namespace="cn.edu.chenggong.dao.NewsAdvertisMapper">
    <!--添加文件及广告信息 -->
    <insert id="addNewsAdver" parameterType="NewsAdvertis">
    	insert into newsadvertis (newsName, newsPath, advertisName, advertisPath) VALUES (#{newsName}, #{newsPath}, #{advertisName}, #{advertisPath});
    </insert>
   <!--  查询表里的所有内容 -->
    <select id="findNewsAdver" resultType="NewsAdvertis" parameterType="NewsAdvertis">
        select * from newsadvertis 
    </select>
</mapper>